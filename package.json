{
  "name": "gpt-translator-backend",
  "version": "1.0.0",
  "description": "Netlify function for translation and TTS",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "dependencies": {
    "node-fetch": "^2.6.7"
  }
}